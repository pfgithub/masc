\.data
\    helloworldtxt: .asciiz "Hello, World!"
\.text

var x: u32 = 5;
x = x + 2;
var y: u32 = 10;

$v0 = 36;
$a0 = x;
\syscall
!clear $call;

x = x + 3;
var z: u32 = 8;

var must_save: u32 = 8;

if z == 8 {
    var should_be_in_t0: u32 = 5;
    $v0 = 4;
    \la $a0, helloworldtxt
    \syscall
    !clear $call;
}

must_save = must_save + 3;

\\# loops will have some implications for register selection, so TODO.